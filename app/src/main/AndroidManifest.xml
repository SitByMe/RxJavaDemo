<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ptv.zohar.rxjavademo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.BasicUseActivity" />
        <activity android:name=".activities.ApiDocActivity" />
        <activity android:name=".activities.use.CountdownActivity" />
        <activity android:name=".activities.UseScenesActivity" />
        <activity android:name=".activities.api.TransformOperatorActivity" />
        <activity android:name=".activities.api.CreateOperatorActivity" />
        <activity android:name=".activities.api.FilterOperationActivity" />
        <activity android:name=".activities.api.CombinationOperationActivity" />
        <activity android:name=".activities.api.ErrorOperationActivity" />
    </application>

</manifest>